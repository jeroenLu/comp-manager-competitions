spring:
#  main:
#    banner-mode: off
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    platform: h2
server:
  port: 8080

